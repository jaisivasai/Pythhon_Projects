L = ((90,10,40), 'JAISIVASAI', (5.0, 20, 300), [82, 45], [1, 2, 3])  # this a List we can change []
print(L[2])
print(L[1][0])
print(L[3][0])
# Tuple example

L[3][0] = 39 # EXAMPLE One
L[4][2] = [100, 200] # EXAMPLE Two

# Tuple is immutable in nature
# In side tuple we can edit only 'list' [] values. In side square brakets value we can edit.
# square brates values only we can edit
# only list we can change the values in tuple

print(L[3])
print(L[4])
